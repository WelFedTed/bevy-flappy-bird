[package]
name = "bevy-flappy-bird"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18"

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 3
